{% extends 'base.html.twig' %}

{% block title %}Almacen{% endblock %}

{% block body %}
    <h1>Almacen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ almacen.id }}</td>
            </tr>
            <tr>
                <th>Locacion</th>
                <td>{{ almacen.locacion }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ almacen.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ almacen.direccion }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ almacen.responsable }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ almacen.telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('almacen_index') }}">back to list</a>

    <a href="{{ path('almacen_edit', {'id': almacen.id}) }}">edit</a>

    {{ include('almacen/_delete_form.html.twig') }}
{% endblock %}
